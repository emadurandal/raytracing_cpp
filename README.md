# Native Build

```bash
$ mkdir build
$ cd build
$ cmake -G Xcode -DGTEST_HAS_PTHREAD=1 ../
```

# Emscripten Build

```bash
$ mkdir embuild
$ cd embuild
$ emcmake cmake ..
$ make
```

# Emscripten Build

```
$ mkdir embuild
$ cd embuild
$ emcmake cmake ..
$ make
``` 
